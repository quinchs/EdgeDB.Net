using EdgeDB.QueryBuilder.StandardLibGenerator.Models;
using System.Linq.Expressions;

namespace EdgeDB.QueryBuilder.StandardLibGenerator
{
    internal class OperatorGenerator
    {
        public Dictionary<string, ExpressionType> ExpressionMap = new Dictionary<string, ExpressionType>
        {

        };

        public static void GenerateOperators(IReadOnlyCollection<Operator> operators)
        {
            
        }
    }
}
